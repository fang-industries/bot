const { Events, MessageReaction } = require("discord.js")
module.exports = {
    name: Event.message,
    async execute(message) {
        const guildMember = interaction.guild.memberCount
        const foundingFather = "1040631470852677744"
        if (MessageReaction.count > guildMember / 2){
        }
    }


}